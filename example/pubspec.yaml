name: example
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  cloud_firestore: ^5.0.2
  cloud_functions: ^5.0.2
  cupertino_icons: ^1.0.6
  firebase_auth: ^5.1.1
  firebase_core: ^3.1.1
  firebase_database: ^11.0.2
  firebase_messaging: ^15.0.2
  firebase_storage: ^12.1.0
  flutter:
    sdk: flutter
  go_router: ^14.2.0
  model_house:
    path: ..
  social_design_system: ^0.0.19

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  test: ^1.25.2

dependency_overrides:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
